<header class="header">
	<div class="container">
		<div class="row header__row header__row_reverse">
			<div class="col-12 col-md-7">
				<div class="header__content">
					<h1 class="header__title color-brand-2">{{post.get_field('section_1_title')}}</h1>
					<p class="header__place">{{post.get_field('section_1_address')}}</p>
					<div class="header__text">{{post.get_field('section_1_text')}}</div>
				</div>
			</div>
			<div class="col-12 col-md-5">
				<div class="header__image">
					<img src="{{TimberImage(post.get_field('section_1_image')).src}}" alt="">
				</div>
			</div>
		</div>
		<div class="row header__banner-row">
			<div class="col-12 col-sm-10">
				{{ function('banner_form', post.get_field('section_1_form_title'), '[contact-form-7 id="29" title="Контактная форма 1" button-hidden-text="Проконсультироваться бесплатно"]')}}
			</div>
		</div>
	</div>
</header>